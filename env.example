# Resend API Configuration
# Get your API key from https://resend.com/api-keys
# Sign up at https://resend.com if you don't have an account
RESEND_API_KEY=re_example_1234567890abcdefghijklmnopqrstuvwxyz

# Email Configuration
# The email address that will appear as the sender (must be verified in Resend)
# For testing, you can use: onboarding@resend.dev (pre-verified by Resend)
# For production, verify your domain in Resend and use: AlphaTON Capital <noreply@yourdomain.com>
RESEND_FROM_EMAIL=AlphaTON Capital <onboarding@resend.dev>

# The email address where form submissions will be sent
# Note: Individual forms have their own hardcoded destinations in the API routes
# This variable is used as a fallback if the hardcoded values need to be overridden
RESEND_TO_EMAIL=admin@yourdomain.com

# Keystatic Admin Authentication
# Set a strong password for accessing the Keystatic admin panel at /keystatic or /admin
# Use a secure password - this is just an example
KEYSTATIC_PASSWORD=your_secure_password_here

# Session secret for signing authentication tokens
# Generate a secure secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Or use a long random string. This should be kept secret and unique per deployment.
# This is just an example - generate your own unique secret
KEYSTATIC_SESSION_SECRET=your_64_character_hexadecimal_secret_key_here_example_only_1234567890abcdef

# Site URL (for metadata, Open Graph, Twitter Cards)
# Use your actual production domain in production
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
